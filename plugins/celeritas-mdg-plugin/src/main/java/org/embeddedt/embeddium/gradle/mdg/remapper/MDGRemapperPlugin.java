package org.embeddedt.embeddium.gradle.mdg.remapper;

import org.gradle.api.Plugin;
import org.gradle.api.Project;

public class MDGRemapperPlugin implements Plugin<Project> {
    @Override
    public void apply(Project project) {
    }
}
